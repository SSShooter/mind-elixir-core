# 2026-09-13

> 说明：本日更早的日志文件随 `.workbuddy/` 目录一起在 git 对象库损坏事故中丢失
> （该目录未纳入版本控制，无任何副本可恢复）。以下是可恢复的关键内容与事故记录。

## Outliner 绑定模式（背景，供后续参考）

- 绑定模式（`new Outliner({ el, mei })`）：ONE data, TWO views。大纲直接引用 `mei.nodeData`（不克隆），
  每次栈变化 `bindToLiveTree()` + `rerenderFromMei()` 重渲染。
- `history` 从 `mei.historyStack` 默认取（无需手传）；缺 `historyStack` 时构造器抛错（需 `allowUndo:true` + await init）。
- `OutlinerMei` 是结构化接口（不运行时不依赖 MindElixir 类）。
- 测试基建：`test.html` 暴露 `window.MindElixir` / `window.Outliner`；
  `MindElixirFixture.initBoundOutliner()` 建绑定实例，**`mind.init` 必须 await**（historyStack 在其内部创建）。

## ⚠️ 严重事故：git 对象库损坏导致 pull 全面失效（已修复）

### 症状
- `git fsck` 报 15 个 `missing blob` + 10 个 `invalid sha1 pointer`（坏 ref）
- `git pull` / `git fetch` **直接 fatal**：`bad object 3f402393...` + `did not send all necessary objects`
- `git status` 报 `fatal: unable to read <sha>`
- 用户以为「最近的提交都没了」

### 根因（**非 AI 造成，是 GitKraken 的 gc/repack 中断**）
- 证据：`.git/gk/config`（GitKraken 标记）显示分支 `next/v6` 于 16:55 被访问；`.git/objects` 目录 17:04；
  两个 pack 文件 17:05 / 17:15 被重写。
- 机制：gc/repack 过程中 loose objects 被清理，但**引用它们的 index 指针和 refs 没同步**，对象悬空。
- **pull 失败的精确原因**：坏 ref `refs/remotes/origin/feat/svg-render → 3f402393...`（对象不存在）。
  git fetch 协商阶段要遍历全部本地 remote refs，读到坏指针就 fatal 中止 → **整个 fetch/pull 全废**。

### 修复步骤（已验证有效）
1. **先备份**：`cp .git/packed-refs .git/refs-backup-20260913/packed-refs.bak`
2. **定位所有坏 ref**：
   ```bash
   git for-each-ref --format='%(refname) %(objectname)' | while read ref sha; do
     git cat-file -e "$sha" 2>/dev/null || echo "BAD $ref $sha"; done
   ```
3. **删除 9 个坏 ref**（3 个 `refs/remotes/origin/*` + 6 个 `refs/tags/*`）：
   `git update-ref -d <refname>` —— 远端有这些分支/tag，fetch 后自动重建，**零数据损失**
4. **fetch 恢复**：`git fetch --all` → 5 个分支全部成功
5. **清残留**：4 个 skill 文件（customize-markdown / integrate-mind-elixir / plaintext-format /
   streaming-mindmap）HEAD 里有但磁盘缺失 → `git checkout HEAD -- skills/<...>`

### 关键事实（下次直接抄）
- **坏 ref 会让 pull 彻底瘫痪**，但它指向的对象本就不存在，删掉零损失 —— **优先修这个**。
- `git fsck` 的 `missing blob` 若集中在 `refs/remotes/*` 和旧 `refs/tags/*`，是历史远端快照被清理，
  **与本地分支无关**；用 `git rev-list --count HEAD` + `git log` 立刻能证明本地提交完好。
- **用户的「提交没了」是误判**：本地 `next/v6` HEAD `38fb23c` 与 `origin/next/v6` 的共同祖先就是它自己，
  `git rev-list --left-right --count next/v6...origin/next/v6` = `0 30`（领先 0 / 落后 30）
  → 只是没拉取，一条都没丢。
- **切勿用 `git restore --source=HEAD -- <dir>` 恢复「HEAD 中不存在的路径」**：它会**删除工作区该路径**，
  引发级联删除（本次把整个 `src/` 删了，靠 `git checkout HEAD -- src/` 才救回 74 个文件）。
  正确恢复命令：`git checkout HEAD -- <path>`。
- `git restore`/底层 unlink 删除的文件**不进回收站**，误删后无法从回收站找回。

### 未解决 / 待确认
- 8 个**从未提交过**的新文件，对象彻底丢失（对象库 + 回收站均无副本）：
  `src/dev.outliner.ts`、`src/outliner/{Outliner,icons,index,operations,types}.ts`、
  `src/outliner/outliner.css`、`src/utils/historyStack.ts`。
  可部分重建：`dist/types/outliner/*.d.ts` + `dist/types/utils/historyStack.d.ts`（Sep12 22:32 构建）保留完整类型；
  `tests/outliner.spec.ts` 在回收站中（`$RIJS66G.ts`，2385 bytes）。
- `.git/logs`（reflog）目录缺失，`git reflog` 无输出 —— 无法靠 reflog 找回任何东西。
- 本次修复的一个改动（Outliner 监听 `mei.bus` 的 `expandNode` 事件以同步折叠）随文件丢失，需在重建时重新应用。

## ✅ 重建完成 + 二次 ref 事故修复（当日 18:30 前）

### 重建产物（全部就位）
- `src/outliner/{Outliner,types,operations,icons,outliner.css,index}.ts`、`src/utils/historyStack.ts`、
  `src/dev.outliner.ts` —— 8 个丢失文件全部重建。
- `src/index.ts`、`src/plugin/operationHistory.ts` 的未提交集成改动重建（导出 `Outliner` / `HistoryStack`，
  `mei.historyStack` 接入，undo/redo/clearHistory 委托给栈）。
- `test.html`、`tests/MindElixirFixture.ts`（`initBoundOutliner()` + `await mind.init()`）补回。
- 保真度（诚实评估）：`Outliner.ts`/`types.ts`/`index.ts`/`dev.outliner.ts` ≈100%（上下文有全文）；
  `historyStack.ts` ≈95%；`operations.ts` ≈80%（d.ts 为旧版，已补 `addSiblingBeforeOperation`）；
  `icons.ts` ≈30%（SVG 路径按 lucide 风格重画）；**`outliner.css` 无原文，按类名重写**。

### 重建引入的回归（已修）
- `Operation History - History Branching` 失败：`restore()` 未同步 `currentSnapshot`，undo 后新 push 的
  `before` 是过期快照。修复：`restore` 中加 `currentSnapshot = snapshot`。修后 operation-history 9/9 过。

### 二次事故：stash 中断导致 HEAD 漂移 + 孤儿 pack（自造，已修）
- 起因：为取 HEAD 基线跑 `git stash push -u`，被 SIGTERM 打断，**HEAD 从 `1f230a5` 漂到 `38fb23c`**
  （`next/v6` 指针被移动），且留下 **72 个 staged 条目**。
- 更糟：留下**孤儿 `pack-aa1d00fc....idx`（无对应 .pack）**，被 multi-pack-index 引用，导致
  `git fsck` 报 `failed to load pack in position 0` + 10+ 条 `failed to load pack entry`。
  idx 里的 72 个对象**数据从未落盘**，`git cat-file -t` 全部 `could not get object info`。
- 修复配方：
  1. 分支指针：**写 ref 的 git 命令在本会话被 shim 异步化（`git update-ref`/`git reset --soft` 都静默无效）**，
     唯一有效手段是**直接编辑 `.git/packed-refs`**（用 Edit 工具，写完整 40 位 SHA，勿凭短 SHA 拼全）。
  2. 孤儿 pack：删 `.git/objects/pack/<hash>.idx` + 删 `multi-pack-index`（两者都先 cp 到 Temp 备份），
     git 下次操作自动重建。删 idx 需 `/usr/bin/rm` + `dangerouslyDisableSandbox`。
  3. **手写 packed-refs 行后必须保持字典序**，否则 `git fsck` 报 `packedRefUnsorted`。
- 结果：`git fsck` **零输出**，`next/v6` = `1f230a5` ✓。

### 意外收获：救回一条重要独立分支
- `git fsck` 发现 `dangling commit 58ca340`（8/11，`refactor(interaction): modularize input handling
  with new InteractionController`，作者 zhoudejie）。引入 `src/interaction/`（controller.ts 601 行等 7 文件），
  把 `src/mouse.ts` 大幅拆分。**父提交不在 HEAD 历史里，是独立分支线**。
- 已建 `rescue/interaction-refactor` 分支保住它（手写 packed-refs 创建）。是否合并待用户决定。

### 验证
- `tsc --noEmit` 通过；outliner/history/undo/expand 定向套件 **29/29 passed**；
  全量套件 **119 passed / 15 failed**（15 个失败全为既有的截图快照尺寸 & 拖拽时序问题，与本次无关）。

## outliner 样式复原 + demo 页（当日 19:00）

### 结论：outliner 的样式真源是 `C:\git\react-outliner`
- 原组件来自 react-outliner（React → 纯 JS 重构）。**类名 100% 沿用**，所以 CSS 可直接移植。
- 真源文件：`C:\git\react-outliner\src\components\OutlineItem.css`（**唯一的样式文件**，
  `Outliner.tsx` 也只是 `import './OutlineItem.css'`；`src/index.css` 仅 3 行 tailwind 指令）。
- 已把 `src/outliner/outliner.css` 重写为该文件的**逐条移植**（变量名 `--rol-*` 原样保留）。
  仅新增末尾 3 条图标尺寸规则（原版用 lucide `<ChevronRight size={14}/>`，这边是裸 SVG，
  用 `.outline-item-collapse-btn svg { width:14px; height:14px }` 等 CSS 统一）。

### 移植时必须保留的关键布局值（改了就错位）
- `.outliner-container { padding: 1.5rem }` **+** `.outliner-items { padding-left: 1.25rem }`
  → 合计 44px 左侧留白，**专为 `.outline-item-btn-group { left: -39px }` 让位**，缺一不可。
- `.outline-item-dot { position:absolute; top:15px; left:9px }`（相对 `.outline-item-container`）。
- `.outline-item-wrapper { align-items: baseline; padding: .25rem .5rem }`。
- `.outline-item-vertical-line` 用 `border-left: 2px solid`（不是 width+background），
  `left` 由 JS 内联 `level*24 - 13`，wrapper `margin-left: level*24`。
- 折叠按钮 `[data-state='hidden']` 是 **`display:none`**（不是 visibility），
  `[data-state='collapsed']` 有 `background: var(--rol-drag-indicator)`。
- 暗色：原版用 `.dark` 类变量覆盖。mind-elixir 侧靠 `theme.type==='dark'` 往 **地图容器**
  设 CSS 变量（`utils/theme.ts`），不会传到 outliner 容器——所以 `.dark` 块保留但需外部加类才生效。

### 新增 `outliner-demo.html`（仓库根）
- 原 file（未提交、随事故丢失、对象库与回收站均无）**无法找回**，按需重写。
- 契约来自 `src/dev.outliner.ts`：需 `#map` `#outline` `#entries` `#btn-undo/redo/clear`
  与 `.entry/.empty/.cursor/.idx/.doc` 类；脚本入口 `<script type="module" src="/src/dev.outliner.ts">`。
- 双栏 grid（导图 | 大纲）+ 底部历史检查器，浅色极简、无阴影。

### 环境坑（本次踩到）
- **端口 5199 被 react-outliner 自己的 vite dev server 占用**，且 Windows 上 `--strictPort`
  仍可能因 IPv4/IPv6 双栈让两个进程同时 listen → 请求打到别人的站点（页面标题变成
  "React Outliner Neo"）。改用 5321 + `--host 127.0.0.1` 才拿到自己的页面。
- 项目只装了 `@playwright/test`，**没有顶层 `playwright`**：脚本要 `require('@playwright/test')`。
- 截图脚本放仓库根会与 git 操作互相干扰（`tmp-shot.cjs` 曾消失），尽量放 Temp 并用 NODE_PATH
  （但 NODE_PATH 对 pnpm 的软链结构不生效 → 放项目内最稳，用完即删）。


## outliner-demo 接入 markdown / KaTeX 渲染（已落地并浏览器实测）

需求：`outliner-demo.html` 的导图与大纲两边都要渲染 markdown + KaTeX（原来导图侧也没有）。
做法（5 个文件，1 新增）：

- **新增 `src/dev.markdown.ts`**：`export function renderMarkdown(text)` = KaTeX 预渲染（先 `$$...$$`
  display，再 `$...$` inline）→ `marked`（自定义 strong/link renderer，`gfm: true`）。
  `marked.use()` 提到模块顶层只调一次（原来在 dev.ts 的 render 函数里每次调用都执行）。
  **KaTeX 必须做预渲染**：marked 会吃掉 LaTeX（`_` 变强调、`\\` 变转义）。
- `src/dev.ts`：内联 markdown 选项删掉，改 `markdown: renderMarkdown`（顺带删掉已不再使用的
  `katex` / `Tokens` / `marked` import；`md2html` 本来就是死 import，没动）。行为不变（实测一致）。
- `src/dev.outliner.ts`：`import 'katex/dist/katex.min.css'` + `mindElixir({ markdown: renderMarkdown })`
  + `new Outliner({ ..., markdown: renderMarkdown })`。两个视图同一个渲染器，所以显示一致。
- `src/outliner/outliner.css`：补全 markdown 内容块样式（原来只有 h1-h6/strong/em/a）：
  压平 p/ul/ol/blockquote/pre/table 的 UA 边距、code/pre 灰底、table 边框、以及
  **`.katex-display { display:inline-block; margin:0; text-align:left }`**——
  KaTeX 的 display 公式默认是居中块，不压平会把大纲的一行撑得很高。
- `outliner-demo.html`：只改了顶栏/栏目提示文案。

顺手修的既有 bug：`strong` renderer 输出 `class="asterisk"` / `"underscore"`，但 `src/markdown.css`
里写的是 `strong.asterisk-emphasis` / `strong.underscore-emphasis`，类名对不上 →
`**bold**` 拿不到 `--selected` 色、`__highlight__` 拿不到黄底（且 `style="background-color: ;"` 是无效声明）。
现在改成 `-emphasis` 后缀，且只在 `%:color:` 显式给色时才输出 style 属性。

### 验证方式（可复用）
- 本机 dev server **常驻在 23333**（`vite.config.ts` 里 `strictPort: true`），用户自己开着：
  **不要另起一个**（会 "Port 23333 is already in use"），直接 curl/浏览器打它即可，vite 会热更模块。
- chrome-devtools MCP 探针比截图快：`document.querySelectorAll('#outline .katex').length` 等
  可直接确认两个视图都渲染了；编辑态确认用 `el.focus()` → textContent 应回到 `# Heading 1` 原文，
  `el.blur()` → 重新渲染成 `<h1>`（实测通过）。
- `pnpm tsc --noEmit` 与 `biome check` 我的三个 ts 文件都干净；`biome check src` 报的 8 个
  format 错误是**既有欠债**（largeMap.ts / index.css / methods.ts / mouse.ts /
  outliner/operations.ts / outliner/outliner.css / plugin/selection.ts / utils/pubsub.ts），
  都是引号风格与 color-mix 换行之类，没去动。

## 方案设计：折叠展开纳入操作历史（用户要求"先给方案"，尚未动代码）

调研结论（源码事实，行号已核对）：
- 折叠状态本就在快照里：`NodeObj.expanded`（src/types/index.ts:184），`getData()`→`collectData`
  直接返回 `instance.nodeData`（interact.ts:8-18 / 99-101），**无需改数据结构**，缺的只是"上报"。
- 唯一入栈入口是 `operation` 事件：plugin/operationHistory.ts:120-129（before=currentSnapshot，after=getData）。
- 折叠/展开完全不 fire `operation`：interact.ts:366-418 只 fire view 事件 `expandNode`；interact.ts:420-438
  的 `expandNodeAll` **什么都不 fire**（既有 bug：bound outliner 因此不同步折叠）。
- 三个手势入口：mouse.ts:184-190（expander 单击 / ⌘+单击→expandNodeAll）、
  keypress.ts:151-182（Ctrl+K 系列直接 `setExpand`+`refresh`，连 expandNode 事件都不 fire）。
- 内部自动展开两处：nodeOperation.ts:43（addChildFunc）、nodeOperation.ts:251（moveNode 'in'）；
  两者之后主操作都会 fire operation，所以能吸收进同一条历史（前提是不额外上报）。
- Outliner：bound 模式靠 stack.subscribe + bus expandNode 双通道同步（Outliner.ts:103-117）；
  独立模式的折叠**已经入历史**（Outliner.ts:167-190 → commit，meta op='reshapeNode'）→ 与 bound 模式不一致。

推荐方案（方案 A）要点：expandNode 增加第三参 `options?: {silent?: boolean}`；写 expanded 后
fire `operation`（name: 'expandNode' | 'collapseNode'，target: node）；expandNodeAll 补 fire 两个事件
（recursive/level）；Ctrl+K 用 target=根节点 一条历史涵盖全图；nodeOperation 两处传 `{silent:true}`；
operationHistory 的 restore 对"视图操作"始终恢复 currentSelected（不选 target）；outliner 两条同步通道
用 queueMicrotask 去抖；改签名后必须 `pnpm tsc && pnpm gen:members`（index.ts:243 是 GENERATED 块，底部有守卫）。

已知局限：focus 模式下 collectData 取 `nodeDataBackup`（interact.ts:10），撤销会刷整树，需单独验证。

## ✅ 折叠/展开纳入操作历史（当日 21:4x 实施完成，测试通过）

用户选定的三项：一手势一条历史 / 含 Ctrl+K 批量 / 默认纳入（不做开关）。

源码改动：
- `utils/pubsub.ts`：新增 `ExpandOperation`（`expandNode | collapseNode` + `recursive`/`level`），并入 `Operation`。
- `types/index.ts`：新增并导出 `ExpandNodeOptions { silent?: boolean }`。
- `interact.ts`：`expandNode(el, isExpand?, options?)` —— 无 children 直接 return（顺带修掉对叶子调用的 TypeError）；
  目标状态与现状相同则短路；DOM 更新后仍 fire `'expandNode'`，非 silent 时再
  fire `('operation', { name: expanded ? 'expandNode' : 'collapseNode', target: node })`。
  `expandNodeAll` 加同款 options、**补 fire `'expandNode'`**（修掉既有「递归展开不同步 outliner」的 bug），
  并用新增的 `wouldExpandChange()` 拦住无变化的批量调用。
- `nodeOperation.ts:43 / :251`：内部自动展开改传 `{ silent: true }` —— 展开并入主操作快照，一次撤销即回滚。
- `plugin/keypress.ts`：Ctrl+K 系列以根节点为目标 fire 一条 operation（`target: nodeData` + recursive/level）。
- `plugin/operationHistory.ts`：`calcCurrentTarget` 显式处理折叠；`restore` 对视图操作恒定恢复 `currentSelected`。
- `outliner/Outliner.ts`：新增 `requestSync`（同步渲染 + 同 tick 去重；渲染必须同步，否则 `applyBoundOperation`
  随后的 `focusItem` 会拿到旧 DOM）；`updateItem` 的 meta op 统一为 expandNode/collapseNode；`types.ts`/`index.ts`
  注释更新；`OutlinerMei.expandNode` 加 options。
- `src/index.ts`：GENERATED 块用 `gen-members.js` 重生成（输出与手改逐字一致 → 说明**手改可行但流程是
  gen:members + biome format**）。
- `CHANGELOG.md`：新增 Unreleased 段（Breaking / Features / Bug Fixes）。

测试（新增 8 例）：折叠可撤销、展开可撤销+重做、递归展开一条历史、叶子不入栈、拖入折叠父节点一次回滚、
折叠与编辑交错、Ctrl+K 全图一条历史、bound outliner 两向撤销同步。
结果：`tsc` 干净；定向 27/27 passed；全量 **142 passed / 1 failed** —— 唯一失败是
`multiple-instance.spec.ts` 截图快照差 1 像素（环境渲染漂移，与本次无关，**未更新基线**）。

环境坑（详见 .workbuddy/memory/MEMORY.md）：无全局 pnpm → corepack + `/tmp/wb-shim/pnpm`；
playwright 1.62.1 需 headless shell 1234；断言撤销深度用 `historyStack.currentIndex`。

## focus 模式成为历史边界（当日 21:40）

需求：进入 / 取消 focus 直接清空历史。

- `interact.ts`：`focusNode` 在 `initRight()` 之后、`cancelFocus` 在 `refresh()` 之后各调
  `this.clearHistory?.()`（可选链 → `allowUndo:false` 时 no-op）。**顺序关键**：cancelFocus 必须先
  refresh 再 clear，否则 re-baseline 的 `getData()` 会取到 focus 子树而非整图。
- 顺带修掉既有缺陷（先写测试让它失败再修）：focus 内撤销走 `refresh(全量快照)` 会把整张图重新渲染
  而 `isFocusMode` 仍为 true，之后 `cancelFocus` 又用陈旧 backup 覆盖 → 撤销结果丢失。
  `operationHistory.restore` 现在：`refresh(snapshot)` → 按 `mei.nodeData.id` 在快照里 `findById`
  取回 focus root → `nodeDataBackup = 全量克隆`、`nodeData = focus 子树` → 再 `refresh()`，别名恢复。
  找不到 root 时降级为退出 focus（实际不可达）。
- `tests/focus-history.spec.ts` 新增 4 例：进入清空 / 离开清空 / focus 内撤销+重做仍在 focus /
  focus 内撤销后退出不被陈旧 backup 复活。临时把 `focusRootId` 置空验证过：后两例确实失败。
- `CHANGELOG.md` 加 Behavior Changes 段；readme 无 undo/focus 章节且 7 个翻译版会漂移，本轮不动。
- 全量 147 例：146 passed / 1 failed，唯一失败仍是 multiple-instance 截图差 1 像素（未更新基线）。
